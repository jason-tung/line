from display import *

def octant(x,y):
    if x > 0 and y > 0:
        
    if x < 0 and y > 0:

    if x < 0 and y < 0:

    if x > 0 and y < 0:

def draw_line( x0, y0, x1, y1, screen, color ):
    
